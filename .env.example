# Copy this file to .env.local for local development and set a real password.
# In production (Vercel/Netlify), set this in the platform's Environment Variables UI —
# never commit the real password to git.
VITE_APP_PASSWORD=changeme123
